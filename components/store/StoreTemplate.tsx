"use client";

/**
 * Compatibility wrapper — Store composition lives in StoreRenderer + Registry.
 * Prefer importing StoreRenderer for new code.
 */
export { StoreRenderer as StoreTemplate } from "@/components/store/StoreRenderer";
