/**
 * Temporary brand config — swap `name` when the final brand is locked.
 * Prefer passing localized names via dictionary for display copy.
 */
export const brandConfig = {
  /** English fallback / product code name */
  name: "Vitrin",
  /** Abstract mark title for accessibility */
  markLabel: "Brand",
} as const;
